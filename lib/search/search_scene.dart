import 'package:flutter/material.dart';

class SearchScene extends StatelessWidget {
  const SearchScene({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Search Scene...");
  }
}
